#include <iostream>
#include <cstdlib>
using namespace std;

int main() {
    for (int i = 0; i < 100; ++i) {
        system("start cmd");  // Opens a new console window (on Windows)
    }
    return 0;
}
